#include <iostream>

using namespace std;

int main()
{
    int n, sum;
    cin>>n;
    for(int i=1; i<n; i++)
    {    
        if(n%i==0)
            sum+=i;
    }
    if(sum==n)
        cout<<"Number is perfect";
    else
        cout<<"Number isn't perfect";

    return 0;
}
