#include <iostream>
#include <string>
using namespace std;
int main()
{
    setlocale(LC_ALL, "rus");
    string S1, word("");
    int n(1), first(0);
    cout<<"Введите строку"<<endl;
    getline(cin, S1);   
    
    return 0;
}
