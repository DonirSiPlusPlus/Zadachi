#include <iostream>
#include <stdlib.h>
#include <iomanip>
using namespace std;
int main()
{
    int mas[100][100], n, m, naib, maybe;
    cin>>n>>m;
    
    for(int i=0; i<n; i++)
    {    
        for(int j=0; j<m; j++)
        {
            mas[i][j]=rand()21-10;  //[-10;10]
            cout<<setw(3)<<mas[i][j];
        }
        cout<<endl;
    }
    
    for(int i=0; i<n; i++)
        for(int j=0; j<m; j++)
            if(mas[i][]==mas)
                    
    return 0;
}
