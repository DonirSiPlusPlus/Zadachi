#include <iostream>
using namespace std;
int main()
{
    int mas1[100], mas2[100], n, m, flag(0);
    cin>>n>>m;
    cout<<"Massiv"<<endl;
    
    for(int i=0; i<n; i++)
        cin>>mas1[i];
    for(int i=0; i<m; i++)
        cin>>mas2[i];
        
    for(int i=0; i<n; i++)
        for(int j=1; j<m; j++)
            if(mas1[i]==mas1[j] || mas2[i]==mas2[j])
                if(mas1[i]==mas1[j])
                {
                    for(int k=i; k<n; k++)
                        mas1[k]=mas1[k+1];
                    n--;
                }
                else
                {
                    for(int k=i; k<m; k++)
                        mas2[k]=mas2[k+1];
                    m--;
                }
    return 0;
}
