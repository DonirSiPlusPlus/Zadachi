#include <iostream>
using namespace std;
int main()
{
    int mas[100], arr[100], n, m, flag(0);
    cin>>n>>m;
    cout<<"Massiv"<<endl;
    
    for(int i=0; i<n; i++)
        cin>>mas[i];
    for(int i=0; i<m; i++)
        cin>>arr[i];
        
    for(int i=0; i<n; i++)
    {
        for(int k=0; k<i; k++)
            if(mas[i]==mas[k])
                break;   
        for(int j=0; j<m; j++)
            if(mas[i]==arr[j])
    
    }
    return 0;
}
