#include <iostream>
using namespace std;
int main()
{
    int mas1[100], mas2[100], n, m, flag(0);
    cin>>n>>m;
    cout<<"Massiv"<<endl;
    
    for(int i=0; i<n; i++)
        cin>>mas1[i];
    for(int i=0; i<m; i++)
        cin>>mas2[i];
        
    for(int i=0; i<n; i++)
    {
        for(int j=0; j<m; j++)
        {
            if(mas[i]==mas[j])
                break;
            else
                flag++;
        }
        if(flag==m)
            cout<<mas[i];
        flag(0);
    }   
    cout<<"Hello World";

    return 0;
}
